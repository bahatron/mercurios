{
    "name": "example",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build:clean": "rm -rf dist && tsc -b --force --verbose",
        "build": "tsc -b --verbose"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@bahatron/utils": "^1.1.2",
        "@types/mocha": "^8.2.2",
        "@types/node": "^16.0.0",
        "fastify": "^3.19.0",
        "luxon": "^1.27.0",
        "mocha": "^9.0.2",
        "typescript": "^4.3.5"
    }
}
